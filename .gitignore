# Node
node_modules/
npm-debug.log*

# Godot editor and cache (legacy project — see README)
.godot/
.import/
.export/

# Local build output
build/

# Godot binary and export templates (never commit these)
Godot_v*
*.tpz
*.zip
*.tar.gz

# Ziva plugin — development tool, not part of the game
addons/ziva_agent/

# OS clutter
.DS_Store
Thumbs.db
*.tmp
*.bak

# Portable bundle produced for handoff (build artifact, see DEPLOY.md)
*.bundle
